# Required for consumer test (pnpm consumer)
# Start the Worker: pnpm dev (from monorepo root)
# Create a token: tila token issue --name consumer-test --created-by dev
TILA_BASE_URL=http://localhost:8787
TILA_TOKEN=your-project-token-here
