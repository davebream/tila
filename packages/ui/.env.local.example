# Local cross-origin dev: Vite runs on :5173, wrangler runs on :8787.
# Copy this file to .env.local and set VITE_API_URL to point at your local Worker.
# Also add http://localhost:5173 to CORS_ALLOWED_ORIGINS in wrangler.toml [vars].
VITE_API_URL=http://localhost:8787
