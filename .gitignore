node_modules/
dist/
.env
.env.local
*.db
*.db-journal
.DS_Store
.turbo/
.wrangler/
*.map
*.tsbuildinfo
.tila/.env
.build-checkpoint.json
build-review-log.yaml
.worktrees/
.kombajn/
.impeccable/

# Dev-only wrangler config (generated by scripts/dev-setup.sh)
wrangler.dev.toml
.dev.vars

# MCP configs (use .example files as templates)
.mcp.json
.cursor/mcp.json
.vscode/mcp.json

# Worker sidecar (generated by build:worker-sidecar for binary packaging)
packages/cli/worker/

# Platform CLI binaries (placed by CI at publish time, never committed)
packages/cli-*/bin/tila
packages/cli-*/bin/tila.exe

# tila
.tila/.session
.tila/wrangler.toml
.tila/lifecycle.json

# tila
.tila/github-app.json
.tila/config.toml

# Local dev/debug artifacts (screenshots, agent transcripts, MCP logs)
# Root-anchored so packages/ui/ image assets stay committable.
/*.png
/screenshots/
/.playwright-mcp/
/2026-*-command-*.txt
/2026-*-caveat*.txt
# Local AI tooling (not published)
.claude/

# kombajn build-loop checkpoint (orchestration artifact, never committed)
.build-checkpoint.json
